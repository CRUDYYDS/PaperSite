// src/environments/environment.prod.ts
export const environment = {
  production: true,
  githubPagesMode: true,
  baseHref: '/PaperSite/', // 替换为你的实际仓库名
  apiBaseUrl: '',
  deployUrl: '/PaperSite/' // 替换为你的实际仓库名
};